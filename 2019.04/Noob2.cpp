// 還沒寫完
#include <iostream>
using namespace std;

int main() {
    int m, n;
    cin >> m >> n;

    int Array[m][n];
    for (int i = 0; i < m ; i++) {
        for (int j = 0; j < n; j++) {
            cin >> Array[i][j];
        }
    }

    int col[m];//行中間數
    for (int i = 0; i < m ; i++) {
        bool flag = true; // 是否有黑出現過了
        for (int j = 0; j < n; j++) {
            if (Array[i])
        }
    }
} 
